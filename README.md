# SignIn---SignUp-Form-With-Post-Creation
In addition to signup and sign, you can now make post and do all CRUD operations.

Before starting the project run "npm init -y" command in terminal for node-modules
